-- Счётчик неудачных попыток ввода кода входа.
-- Без него шестизначный код можно было перебирать все десять минут его жизни:
-- ограничения были только на отправку нового кода, но не на проверку.
ALTER TABLE login_codes ADD COLUMN IF NOT EXISTS attempts INTEGER NOT NULL DEFAULT 0;
